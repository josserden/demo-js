/*
 * Перевірити, чи є число цілим
 */

const num = 4;

// if (num % 1 === 0) {
//   console.log('це ціле число');
// }

// let result = num % 1 === 0 ? 'це ціле число' : 'це не ціле число';

// console.log(result);
